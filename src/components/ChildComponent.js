const ChildCompnent = (props) =>{
    
        return(
        <>
        <button onClick={()=>props.greetParen1t('abc')}>Greet Parent</button>
        </>
    )}
    
    export default ChildCompnent